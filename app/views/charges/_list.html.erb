<script>
$(document).ready(function() {
  $("#new_charge").submit(function(event) {
        $('#new_charge').get(0).submit();
  });
    $("#new_charge").submit(function(event) {
        $('#new_charge').get(0).submit();
  });
});
</script>

		<h3> Charges</h3>
			<div class="pull-right" style="margin-top:-40px;">
				  <%= link_to 'New charge', new_studio_charge_path(@studio),
        :class => 'btn btn-primary btn-sm' ,  :data => {:toggle => "modal", :target => "#chargeModal"} , :remote => true, :html => {:style => "margin-top:5px;"}%>

			</div>
			<div class="divider-horizontal-regular"></div>
				<table class="table table-hover">
				    <thead>
				        <tr>
				          <th>Name</th>
				          <th>Price</th>  
				          <th>Interval</th>
				          <th>Interval Count</th>
				          <th>Trial Period Days</th>                        <th></th>
				          <th></th>                          
				        </tr>
				    </thead>
				    <tbody>
				    	<% @charges = @studio.charges.find(:all)%>
				        <% @charges.each do |charge| %>
				        	<tr>
				        		<td><%= charge.name %></td>
				        		<td><%= charge.price%></td>
				        		<td><%= charge.interval%>
				        		</td>						
				        		<td><%= charge.interval_count if charge.interval_count?%></td>		        		<td><%= charge.trial_period_days if charge.trial_period_days?%></td>
								<% @edit_charge = charge %>
				  				<td><%= link_to 'edit', edit_studio_charge_path(@studio, @edit_charge),  :data => {:toggle => "modal", :target => "#editModal"} , :remote => true %>	</td>
    							<td><%= link_to 'Delete', [charge.studio, charge], method: :delete, data: { confirm: 'Are you sure? This action cannot be undone.' }, :class => 'btn btn-sm' %></td>
				        	</tr>
				        <% end %>
				</table>




  <div class="modal fade" id="chargeModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Create Charge</h4>
      </div>
 		<%= render '/charges/form' %>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->

