<script> 
$(document).ready(function() {

    // page is now ready, initialize the calendar...
$('.dropdown-toggle').dropdown()

});
</script>

          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Studio Manager", root_path, class: "navbar-brand"%>
        </div>
        <div class="navbar-collapse collapse">

          <% if user_signed_in? %>

            <ul class="nav navbar-nav navbar-right">
            <li class="dropdown" role="menu">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                  <% if current_user.has_role? :instructor, :any %>
                  <li><%= link_to 'View Profile', user_path(current_user) %></li>
                  <% end %>
                  <% if current_user.has_role? :student, :any %>
                  <li><a>View History</a></li>
                  <% end %>
                  <% if current_user.has_role? :admin, :any %>
                    <% if current_user.has_role? :owner %>
                    <% @studio = Studio.first %>
                  <li><%= link_to 'Edit Studio', edit_studio_path(@studio) %> </li>
                  <% end %>
                  <% end %>
                  <li><%= link_to 'Edit Account', edit_user_registration_path %></li>
                  <div class="divider"></div>
                  <li><%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %></li>
              </ul>
            </li>
          </ul>

          <% else %>
          <form class="navbar-form navbar-right">
            <div class="form-group">
              <input type="text" placeholder="Email" class="form-control input-sm">
            </div>
            <div class="form-group">
              <input type="password" placeholder="Password" class="form-control input-sm">
            </div>
            <button type="submit" class="btn btn-sm btn-default">Sign in</button>

            <%= link_to 'Login', new_user_session_path(), type: "button", class: 'btn btn-sm btn-default'  %>

            <%= link_to 'Register', new_user_registration_path(), type: "button", class: 'btn btn-sm btn-default'  %>

          </form>
          <% end %>

