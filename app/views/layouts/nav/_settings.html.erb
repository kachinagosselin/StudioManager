
<% if user_signed_in? %>
<nav class="main-nav">
	<div class="container">
		<ul>
			<li><%= link_to raw("<span class=\"glyphicon glyphicon-home\"></span>".html_safe), root_path() %></li>
    		<li><%= link_to 'Accounts Settings', edit_user_registration_path() %></li>

            <% if current_user.account.present? && !current_user.account.studio.present?%>
            <li><%= link_to 'Add Studio', staff_index_path %></li>
            <% elsif current_user.account.present? && current_user.account.studio.present?%>
            <li><%= link_to 'Studio', settings_studio_path() %></li>
            <% end %>

            <li><%= link_to 'Profile', settings_profile_path() %></li>
            <li><%= link_to 'History', history_user_path(current_user) %></li>
    		<li><a>Notifcations</a></li>

            <% if !current_user.account.present? %>
    		<li><%= link_to 'Upgrade Services', settings_upgrade_path() %></li>
            <% end %>
    		<li><%= link_to 'Support', support_path() %></li>
		</ul>
	</div>
</nav>
<% end %>

			
