<script> 
$(document).ready(function() {

    //NOTE that I am selecting on a class here
    $('#toggle').click(function(e) {
        //You also need to prevent the default behaviour of 
        e.preventDefault();

        $("#container").toggle();
    });

    });
</script>


  <div class="col-lg-8 col-lg-offset-2">

    <h3> Reports</h3>
    <hr>

    <h4> Attendence this Week</h4>
    <hr>

<img src="<%= Gchart.line( :data => [17, 17, 11, 8, 2], 
              :axis_with_labels => ['x', 'y'], 
              :axis_labels => [((Date.today - 7).to_date..Date.today.to_date).map{ |date| date.strftime("%b %d")}], 
              :axis_range => [nil, [2,17,5]], 
            :width => "900", :height => "300")%>"/>
        </br>
            <% total = 0 %>
            <% @events = @studio.events.where('start_at >= ?', Date.today-7) %>
            <% @events.each do |event| %>
            <% total = total + event.users.count %>
            <% end %>
            <p>Total # Students: 
            <%= total %>
            </p>
            <p>Revenue this Week: </p>
            <p>Total # Students: </p>
</div>

